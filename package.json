{
  "name": "doubleword-opencode-ocx",
  "version": "0.2.1",
  "private": true,
  "description": "Static ocx registry for using Doubleword in opencode, served as a Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260710.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
